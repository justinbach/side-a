# Supabase
# For local development, run `npm run db:start` then `npm run db:status` to get these values
# Local defaults (from `supabase start`):
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-from-supabase-status
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-from-supabase-status

# Backend API (for album recognition)
NEXT_PUBLIC_BACKEND_URL=http://localhost:3001

# Resend (for invite emails - optional)
# Get your API key at https://resend.com
RESEND_API_KEY=

# App URL (for email links)
NEXT_PUBLIC_APP_URL=http://localhost:3000
